# Open Questions

## Product

- Should the next storage step be JSON export/import first, or SQLite-backed
  self-host sync first?
- What is the minimum BYOK AI feature that is useful without turning the app
  into a chat product?
- Should local Codex account connection be an integration, a plugin, or only a
  documented workflow?

## Technical

- What migration path should move existing browser-local data into a future
  self-host store?
- Which provider API should be the first BYOK adapter?
- Should GitHub Pages host a static marketing page, or should the repository
  README remain the primary public page for now?
